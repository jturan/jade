{
  "description": "Notifies through herdr when a Claude Code session goes idle, so an orchestrator pane left running does not wait silently.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/notify.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
